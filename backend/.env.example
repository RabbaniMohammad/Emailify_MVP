# Example environment variables for the backend
# Copy this file to `.env` and fill in real values. Do NOT commit your real `.env`.

# --- Instagram / Meta (for Instagram Messaging and Messenger) ---
# Instagram access token (Page or IG user access token)
INSTAGRAM_ACCESS_TOKEN=
# Instagram Business Account ID (IG user id)
INSTAGRAM_IG_USER_ID=
# Alternatively, connected Facebook Page ID (some flows use a page id)
INSTAGRAM_PAGE_ID=

# Messenger (Facebook Page) access token
MESSENGER_PAGE_ACCESS_TOKEN=
MESSENGER_PAGE_ID=

# Webhook verification token (your choice) used to verify meta webhook subscription
META_WEBHOOK_VERIFY_TOKEN=replace-with-a-secure-random-string
# Meta Graph API version to use
META_API_VERSION=17.0

# When you are approved and ready to test, add the above tokens and refresh long-lived tokens as needed.
