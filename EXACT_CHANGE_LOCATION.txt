════════════════════════════════════════════════════════════════════════════════
                         🎯 EXACT LOCATION TO CHANGE
════════════════════════════════════════════════════════════════════════════════

FILE: frontend/src/app/app/features/qa/services/qa.service.ts

════════════════════════════════════════════════════════════════════════════════

LINE 572-584 (Current Code):

  checkTemplateGrammar(html: string): Observable<{
    hasErrors: boolean;
    mistakes: Array<{ word: string; suggestion: string; context: string }>;
    count: number;
    message: string;
  }> {
    return this.http.post<{
      hasErrors: boolean;
      mistakes: Array<{ word: string; suggestion: string; context: string }>;
      count: number;
      message: string;
    }>('/api/qa/template/grammar-check', { html });  // ← CHANGE THIS LINE
                ^^^^^^^^^^^^^^^^^^^^^^^^^^
                        CHANGE THIS URL
  }

════════════════════════════════════════════════════════════════════════════════

CHANGE LINE 583 FROM:
    }>('/api/qa/template/grammar-check', { html });

TO:
    }>('/api/qa-advanced/grammar-check', { html });
              ^^^^^^^^^^^^
              ADD THIS PART

════════════════════════════════════════════════════════════════════════════════

AFTER CHANGE (Line 572-584):

  checkTemplateGrammar(html: string): Observable<{
    hasErrors: boolean;
    mistakes: Array<{ word: string; suggestion: string; context: string }>;
    count: number;
    message: string;
  }> {
    return this.http.post<{
      hasErrors: boolean;
      mistakes: Array<{ word: string; suggestion: string; context: string }>;
      count: number;
      message: string;
    }>('/api/qa-advanced/grammar-check', { html });  // ← CHANGED!
                ^^^^^^^^^^^^
                   NEW!
  }

════════════════════════════════════════════════════════════════════════════════

THAT'S IT! Just add "-advanced" to the URL!

OLD: /api/qa/template/grammar-check
NEW: /api/qa-advanced/grammar-check

════════════════════════════════════════════════════════════════════════════════

TO TEST:
1. Save the file
2. Backend should already be running (npm run dev)
3. Go to browser, open QA page
4. Click "Run Tests"
5. Check backend terminal for logs 🔍
6. Check browser console (F12) for speed ⚡

════════════════════════════════════════════════════════════════════════════════
